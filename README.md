Clone this repository ```git clone git@github.com:mollyseith/contact-book.git```

Run ```npm install```

Create an ```.env``` file in project's root directory

Navigate to ```.env``` file, store API token as ```REACT_APP_API_KEY```

Run ```npm start```
